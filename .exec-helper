commands:
    build: Build the container

patterns:
    TAG:
        default-values:
            - bverhagen/pacman-aur-wrapper:MANUAL

build:
    - command-line-command

command-line-command:
    build:
        patterns:
            - TAG
        command-line: [ docker, build, --tag, "{TAG}", .]
